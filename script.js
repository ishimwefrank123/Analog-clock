const numberHours = document.querySelector('.index.html');

const numberElement = [];

// create the number hours
for(let i=1; i <= 12; i++){
  numberElement.push(
    `<span><p>${i}</p></span>`
  );
  console.log(numberElement)
}

